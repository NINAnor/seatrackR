# Retrieve info on the registered species in the database

Retrieve info on the registered species in the database

## Usage

``` r
getSpecies()
```

## Value

A tibble of the metadata.subspecies table

## Examples

``` r
if (FALSE) { # \dontrun{
getSpecies()
} # }
```
