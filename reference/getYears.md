# Retrieve info on the years where we have position data

This lists the available years and the abbreviation used to identify
them, for example in the postable.

## Usage

``` r
getYears()
```

## Value

A tibble of the years represented in the database

## Examples

``` r
if (FALSE) { # \dontrun{
getYears()
} # }
```
